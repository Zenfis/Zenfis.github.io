source "https://rubygems.org"

# Тот же гем, что использует сама GitHub Pages —
# так сайт локально соберётся ИДЕНТИЧНО тому, что окажется на хостинге.
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-sitemap"
end

# Нужно для Ruby 3.0+
gem "webrick"
